- Added `tests/fixtures/headroom_influence_fixtures.py` with deterministic builders for symbol-search shaping and pytest-output shaping.
- Added `tests/fixtures/test_headroom_influence_fixtures.py` to prove empty results, large exact-match ordering, definition/reference distinctions, failing-assertion data, traceback root cause data, environmental failure data, non-UTF8-safe sentinel text, and very long single-line output.
- The helper module is pure data only and does not mutate `tests/fixtures/python-basic` or any checked-in analyzed fixture source.
- Task 2 preservation stayed deterministic by ranking on normalized kind plus finding severity, and token estimates used surrogate-safe UTF-8 bytes with an explicit basis string.
- Added `src/codescent/core/output_formatter.py` with a fixture-shaped `TestOutputRecord` validator and `ResponseEnvelope` output so failing test names, assertion text, traceback root cause, traceback frames, relevant files, rerun command, and deterministic/environmental classification stay in the first preserved item.
- Long passing output now summarizes through `EnvelopeMode.SUMMARIZED` with `omitted_count`, `original_result_id`, and `retrieval_hints`, while empty output stays exact and bounded.
- Task 9 symbol formatter carries cursor and next-cursor values through envelope stats when provided, and emits retrieval hints without storing full results unless an upstream `original_result_id` is supplied.

- Task 8 stats aggregation is event-only for the MVP slice: empty sessions return zero-valued bounded stats, repeated broad queries group by tool plus normalized fingerprint, and largest summarized results include fingerprints/metrics rather than raw query text.

- Task 7 result retrieval normalizes stored JSON into item sequences by preferring `items`, `results`, `matches`, `symbols`, then `records`; otherwise it treats the full JSON value as one item. Exact retrieval over the explicit limit downgrades the response mode to `summary` with a warning instead of returning unbounded content. Filtering is recursive over stored item fields only: query searches stored text values, file checks `file`/`path`/`file_path`, symbol checks `symbol`/`name`/`qualified_name`/`reference_text`/`call_text`, and result type checks `type`/`kind`/`result_type`.

- Task 13 integration keeps source-range/edit-context tools exact: only `find_symbol` now envelopes symbol search results, and `get_symbol_context`/reference graph tools retain their prior payloads. Contract coverage proves a 20-symbol `find_symbol` response stores 20 raw items and summarizes with positive omissions, while a one-symbol response stays exact with no `original_result_id` and no omission warning.

- Task 11 retry: `retrieve_result` now records `result_retrieved` only after successful service retrieval and only when `session_id` is supplied. The MCP payload sent to `SessionEventRepository` contains `input` metadata for fingerprinting plus `returned_tokens`, `result_count`, `warning_count`, and `exact_requested`; repository sanitization drops raw filter text and stores only metrics/fingerprints. Missing/invalid result errors still return `ResultStoreError.to_payload()` and do not create retrieval events.

- Task 15 storage edge cases: fresh/idempotent init already created v5 `stored_results` and `session_events`, so new coverage focused on deterministic repository restart retrieval, injected-`now` TTL expiry/cleanup, oversized JSON payload persistence without source mutation, v4-to-v5 post-migration writes through `StoredResultRepository`/`SessionEventRepository`, and CLI reset preserving non-target `.codescent`-like paths.

- Task 16 security tests now cover the context optimization guardrails in `tests/security/test_runtime_safety.py`: `find_symbol`/`retrieve_result`/`context_stats` run without socket creation, do not mutate temp repo source hashes, keep `retrieve_result(file=...)` constrained to stored JSON fields even for absolute/traversal-looking filters, allow fake sentinel content only in exact raw-result retrieval, and keep stats/session events plus invalid-ID errors sanitized and traceback-free.

- Task 14 lockstep now treats `retrieve_result` and `context_stats` as live registered MCP tools in docs and contract tests, documents `find_symbol` as a bounded envelope, and keeps `project_guidance`, `project_learnings`, `compress_generic_output`, and `retrieve_original_output` absent from the public surface.

- Task 17 E2E contract coverage now drives the live in-process FastMCP workflow from a 20-symbol `find_symbol` summarized envelope through filtered `retrieve_result` and `context_stats`, proving the same `project_id`/`session_id` records tool calls, summaries, retrievals, and positive token avoidance.

- Task 18 quality gates stayed green after one docs-contract fix in `docs/language-packs.md` added the expected `tree-sitter` mention, and one formatter-related lint failure in `tests/unit/test_output_formatter.py` was resolved by shortening the test name so `ruff format` and `ruff check` agree.
