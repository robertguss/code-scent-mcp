2026-06-13 Task 4 decision: staged `retrieve_result` and `context_stats` as locked post-MVP MCP surface entries in `src/codescent/core/public_surface.py` with `registered=False`, added matching docs placeholders in `docs/mcp-tools.md`, and kept `src/codescent/mcp/server.py` unchanged so the live MCP surface stays MVP-only until Task 14.

Post-MVP exclusions remain explicit: `project_guidance`, `project_learnings`, `record_agent_failure`, `record_successful_recovery`, optional Headroom integration, and generic compression tools are not registered or documented as current surface.

Contract expectations now assert the locked placeholders are declared but not exposed by `fastmcp.Client`, which gives Task 14 a clean registration target without a temporary skipped-test branch.
2026-06-13: Storage schema advanced to v5 with `stored_results` and `session_events` added as explicit migration 5 tables. Fresh init and v4 upgrade both rely on the same table definitions, and the v4 seed fixture now includes `subjective_findings` so the migration test can prove older data survives the upgrade.
2026-06-13 Task 9 decision: symbol-search formatting is a pure core formatter in `src/codescent/core/symbol_formatter.py`, not wired into MCP yet. Defaults are 6 groups and 2 symbols per group; grouping keys are match type, role, path, and module, while symbol types stay preserved as group metadata and item fields. Exact matches and definitions sort before partial/reference results, then higher score/relevance, path, line, kind, and qualified name. Missing `match_type`, `role`, or `kind` emits warnings and uses `unknown` buckets rather than invented classifications. Empty results stay `mode="exact"` with omitted count 0 and no stored result ID.
2026-06-13 Task 6 decision: Added `StoredResultRepository` in `src/codescent/storage/repositories/stored_results.py` with `create_result(StoredResultCreate)`, `get_result(result_id, include_expired=False, now=None)`, `increment_retrieval_count(result_id)`, `cleanup_expired(now=None)`, and `list_summarized_results(limit, order_by, now=None)`. Result IDs are generated as `ctx_` plus a 16-character SHA-256 prefix over project/tool/input/raw/created-at data, and all JSON payloads are stored as exact text for Task 7 service parsing.

2026-06-13 Task 8 decision: implemented `SessionEventRepository` as the only writer for `session_events`, with write-time payload sanitization that stores token/count metrics, booleans, warning codes, and normalized SHA-256 fingerprints instead of raw query/source/result strings. `ContextStatsService` reads bounded local events only and does not expose MCP surface.

2026-06-13 Task 7 decision: implemented `ResultStoreService` in `src/codescent/services/result_store.py` as the service boundary over `StoredResultRepository`. Public service methods are `store_result(...)`, `store_result_json(...)`, and `retrieve_result(result_id, mode="exact"|"summary"|"filtered"|"sample", limit=..., query=..., file=..., symbol=..., result_type=..., now=...)`. Retrieval validates opaque `ctx_` IDs, reads only stored JSON text, increments retrieval count on successful non-summary item retrieval, and never reruns searches or reads filesystem paths from filters.

Task 7 error shape for Task 11 MCP mapping: `ResultStoreError.to_payload()` returns `{"kind":"result_store_error","code":"invalid_result_id"|"missing_result"|"expired_result","message":...,"result_id":...,"retryable":false}`. Invalid IDs are rejected before storage lookup; missing and expired IDs are distinguished by an include-expired repository lookup without exposing tracebacks.

2026-06-13 Task 13 decision: integrated the summarized result envelope only on MCP `find_symbol`, leaving graph/search tools on their existing exact/cursor contracts. The MCP adapter uses `format_symbol_search_results` with a 600-token serialized raw-result threshold; responses over that threshold store the full bounded raw result via `ResultStoreService` and return a compact grouped envelope with a `ctx_` original result id. Smaller symbol responses still return the same envelope fields with `mode="exact"`, omitted count 0, and no retrieval warning. Optional `project_id`/`session_id` parameters record sanitized `tool_called` and `large_result_summarized` events when provided, but no raw query/source text is persisted in event payloads.

2026-06-13 Task 11 decision: registered live MCP `retrieve_result` via a dedicated thin wrapper in `src/codescent/mcp/result_tools.py` and `src/codescent/mcp/server.py`, promoting only `retrieve_result` from locked to registered post-MVP surface while leaving `context_stats` locked for Task 12. The wrapper delegates all retrieval, filtering, limiting, and ID error shaping to `ResultStoreService.retrieve_result(...)`; it catches only `ResultStoreError` and returns `to_payload()` so missing/invalid/expired IDs remain parseable JSON without traceback text. Retrieval telemetry was not added because there is no service-level event API with MCP session/project context; direct low-level event repository writes would couple the MCP adapter to telemetry storage and risk inventing context.

2026-06-13 Task 12 decision: registered `context_stats` as the only new live Headroom MCP stats tool via a thin `src/codescent/mcp/session_stats_tools.py` adapter over `ContextStatsService.context_stats(project_id=..., session_id=...)`. The adapter returns the service payload directly, reads only sanitized `.codescent` session events, preserves Task 11 `retrieve_result` registration unchanged, and exposes fingerprints/metrics rather than raw queries, source, or result payloads.

2026-06-13 Task 17 decision: added a focused workflow contract test rather than expanding the single-tool contracts, so Tasks 11-13 keep unit-like coverage while Task 17 owns the cross-tool `ctx_` round trip and deterministic negative paths.
