# Implementation Plans

Generated by the improve skill on 2026-06-14. Execute in the order below unless dependencies say otherwise. Each executor must read the plan fully before starting, honor its STOP conditions, and update the status row here when done.

## Execution Order & Status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Fix file-context related files | P1 | S | - | TODO |
| 002 | Reduce changed-source test false positives | P1 | S/M | 001 | TODO |
| 003 | Batch multi-query content search | P1 | M | - | TODO |
| 004 | Use the persisted index for context lookups | P1 | L | 001, 003 | TODO |
| 005 | Bound source-file input reads | P1 | M/L | 004 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale).

## Dependency Notes

- 001 lands first because it adds characterization tests around the current file-context contract before broader context refactors.
- 002 depends on 001 because both touch context/test-discovery behavior; fixing the noisy test heuristic before CI/diff work keeps changed-file health trustworthy.
- 003 is independent of 001 and 002, but it should land before 004 if possible so the larger context refactor does not also carry search batching changes.
- 004 depends on 001 and 003 because it replaces hot context paths with persisted-index reads and needs stable behavior tests first.
- 005 depends on 004 because bounded-read changes touch many of the same search/context/rule paths; doing it after the index-backed refactor reduces conflict and makes the remaining reads easier to cap.

## Recommended Verification Baseline

Use the most specific plan-level tests first, then widen to these repo gates when a plan is complete:

| Purpose | Command | Expected on success |
|---------|---------|---------------------|
| Tests | `uv run pytest` | exit 0 |
| Lint | `uv run ruff check .` | exit 0 |
| Format | `uv run ruff format --check .` | exit 0 |
| Typecheck | `uv run basedpyright` | exit 0 |

## Findings Considered And Rejected Or Deferred

- Git related-file lookup shells out per historical commit: real performance concern, deferred because 004 changes related-file internals and should be completed first.
- Dashboard smoke is macOS-specific: valid DX issue, not in this recommended engineering plan set because it is outside the core context/search/test-health dependency chain.
- Real-repo smoke docs contain a personal absolute path: valid docs issue, deferred for a small docs cleanup pass.
- `pydantic-settings` appears unused: valid dependency cleanup, intentionally omitted from this set because it is independent and low risk.
- Stale `Headroom` wording and settled PRD questions: valid docs cleanup, not part of this implementation sequence.
