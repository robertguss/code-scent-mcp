"""A tidy, well-structured module (clean negative for most rules)."""

from __future__ import annotations


def add(first: int, second: int) -> int:
    return first + second


def describe(value: int) -> str:
    return f"value is {value}"
